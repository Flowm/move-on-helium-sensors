#!/usr/bin/env python3
# DisConnect RBF.

import RPi.GPIO as GPIO
import time

CHARGE_PIN = 27
RBF_PIN = 22

GPIO.setmode(GPIO.BCM)
GPIO.setup(RBF_PIN, GPIO.OUT)

GPIO.output(RBF_PIN, GPIO.LOW)
GPIO.cleanup()
